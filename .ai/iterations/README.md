# Iteration Logs

Store one markdown file per meaningful work session.

Recommended filename format: `YYYY-MM-DD-HHMM-short-topic.md`.

Each log should capture:
- what changed
- why it changed
- verification that was run
- open follow-ups
