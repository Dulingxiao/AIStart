# Mission

AIStart exists to give every new fullstack project a clean, reusable AI-first starting point.

The repository standardizes project intent, working constraints, iteration memory, and bridge files so different AI tools read the same facts before making changes.
