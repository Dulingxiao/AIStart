# Success Criteria

V1 is successful when:
- the scaffold can be cloned into a clean new project
- all AI bridge files point to the same canonical context
- the repository contains a minimal runnable web and API skeleton
- governance scripts can create structured task, ADR, and iteration records
- drift checks surface misalignment against mission, scope, and non-goals
